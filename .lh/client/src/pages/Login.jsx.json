{
    "sourceFile": "client/src/pages/Login.jsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 15,
            "patches": [
                {
                    "date": 1752155001359,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1752160291028,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,12 +1,24 @@\n-import React, { useState } from \"react\";\r\n+import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n+import { io } from \"socket.io-client\";\r\n \r\n+const socket = io(\"http://localhost:3000\");\r\n+\r\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n+  const [roomList, setRoomList] = useState([]);\r\n   const navigate = useNavigate();\r\n \r\n+  useEffect(() => {\r\n+    socket.on(\"room-list\", (data) => {\r\n+      setRoomList(data);\r\n+    });\r\n+\r\n+    return () => socket.disconnect();\r\n+  }, []);\r\n+\r\n   const handleJoin = () => {\r\n     if (username && room) {\r\n       navigate(`/chat?username=${username}&room=${room}`);\r\n     }\r\n@@ -22,15 +34,20 @@\n           className=\"w-full p-2 mb-3 border rounded\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n-        <input\r\n-          type=\"text\"\r\n-          placeholder=\"Enter room\"\r\n+        <select\r\n           className=\"w-full p-2 mb-4 border rounded\"\r\n           value={room}\r\n           onChange={(e) => setRoom(e.target.value)}\r\n-        />\r\n+        >\r\n+          <option value=\"\">Select a Room</option>\r\n+          {roomList.map((r) => (\r\n+            <option key={r} value={r}>\r\n+              {r}\r\n+            </option>\r\n+          ))}\r\n+        </select>\r\n         <button\r\n           className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n           onClick={handleJoin}\r\n         >\r\n"
                },
                {
                    "date": 1752160553836,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -7,8 +7,9 @@\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n   const [roomList, setRoomList] = useState([]);\r\n+  const [newRoom, setNewRoom] = useState(\"\");\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n     socket.on(\"room-list\", (data) => {\r\n@@ -23,8 +24,15 @@\n       navigate(`/chat?username=${username}&room=${room}`);\r\n     }\r\n   };\r\n \r\n+  const createRoom = () => {\r\n+    if (newRoom.trim() && !roomList.includes(newRoom)) {\r\n+      socket.emit(\"create-room\", newRoom.trim());\r\n+      setNewRoom(\"\");\r\n+    }\r\n+  };\r\n+\r\n   return (\r\n     <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n       <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n         <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n@@ -34,20 +42,40 @@\n           className=\"w-full p-2 mb-3 border rounded\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n-        <select\r\n-          className=\"w-full p-2 mb-4 border rounded\"\r\n-          value={room}\r\n-          onChange={(e) => setRoom(e.target.value)}\r\n-        >\r\n-          <option value=\"\">Select a Room</option>\r\n-          {roomList.map((r) => (\r\n-            <option key={r} value={r}>\r\n-              {r}\r\n-            </option>\r\n-          ))}\r\n-        </select>\r\n+\r\n+        <div className=\"mb-4\">\r\n+          <select\r\n+            className=\"w-full p-2 mb-2 border rounded\"\r\n+            value={room}\r\n+            onChange={(e) => setRoom(e.target.value)}\r\n+          >\r\n+            <option value=\"\">Select a Room</option>\r\n+            {roomList.map((r) => (\r\n+              <option key={r} value={r}>\r\n+                {r}\r\n+              </option>\r\n+            ))}\r\n+          </select>\r\n+\r\n+          <div className=\"flex gap-2\">\r\n+            <input\r\n+              type=\"text\"\r\n+              placeholder=\"Create new room\"\r\n+              className=\"w-full p-2 border rounded\"\r\n+              value={newRoom}\r\n+              onChange={(e) => setNewRoom(e.target.value)}\r\n+            />\r\n+            <button\r\n+              onClick={createRoom}\r\n+              className=\"bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600\"\r\n+            >\r\n+              +\r\n+            </button>\r\n+          </div>\r\n+        </div>\r\n+\r\n         <button\r\n           className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n           onClick={handleJoin}\r\n         >\r\n"
                },
                {
                    "date": 1752160888737,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,81 +1,106 @@\n import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n import { io } from \"socket.io-client\";\r\n \r\n-const socket = io(\"http://localhost:3000\");\r\n+// ⚠️ Singleton pattern for socket\r\n+const socket = io(\"http://localhost:3000\", { autoConnect: true });\r\n \r\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n   const [roomList, setRoomList] = useState([]);\r\n   const [newRoom, setNewRoom] = useState(\"\");\r\n+\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n+    // 🧠 Listen for updated room list\r\n     socket.on(\"room-list\", (data) => {\r\n       setRoomList(data);\r\n     });\r\n \r\n-    return () => socket.disconnect();\r\n+    // 🔌 Initial emit in case socket connected after page load\r\n+    socket.emit(\"get-rooms\");\r\n+\r\n+    // 🧹 Cleanup\r\n+    return () => {\r\n+      socket.off(\"room-list\");\r\n+    };\r\n   }, []);\r\n \r\n+  // 🎯 Join selected room\r\n   const handleJoin = () => {\r\n-    if (username && room) {\r\n-      navigate(`/chat?username=${username}&room=${room}`);\r\n+    if (!username.trim() || !room.trim()) {\r\n+      alert(\"Please enter username and select a room\");\r\n+      return;\r\n     }\r\n+    navigate(`/chat?username=${username.trim()}&room=${room}`);\r\n   };\r\n \r\n+  // ➕ Create a new room\r\n   const createRoom = () => {\r\n-    if (newRoom.trim() && !roomList.includes(newRoom)) {\r\n-      socket.emit(\"create-room\", newRoom.trim());\r\n-      setNewRoom(\"\");\r\n+    const trimmedRoom = newRoom.trim();\r\n+    if (!trimmedRoom) return;\r\n+\r\n+    if (roomList.includes(trimmedRoom)) {\r\n+      alert(\"Room already exists!\");\r\n+      return;\r\n     }\r\n+\r\n+    socket.emit(\"create-room\", trimmedRoom);\r\n+    setNewRoom(\"\");\r\n+    setRoom(trimmedRoom);\r\n   };\r\n \r\n   return (\r\n-    <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n-      <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n-        <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n+    <div className=\"h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 to-blue-200\">\r\n+      <div className=\"bg-white p-6 rounded-lg shadow-lg w-full max-w-sm\">\r\n+        <h2 className=\"text-2xl font-bold mb-4 text-center text-blue-700\">\r\n+          Join a Chat Room\r\n+        </h2>\r\n+\r\n+        {/* Username Input */}\r\n         <input\r\n           type=\"text\"\r\n           placeholder=\"Enter username\"\r\n-          className=\"w-full p-2 mb-3 border rounded\"\r\n+          className=\"w-full p-2 mb-4 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n \r\n-        <div className=\"mb-4\">\r\n-          <select\r\n-            className=\"w-full p-2 mb-2 border rounded\"\r\n-            value={room}\r\n-            onChange={(e) => setRoom(e.target.value)}\r\n+        {/* Room Selection */}\r\n+        <select\r\n+          className=\"w-full p-2 mb-3 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400\"\r\n+          value={room}\r\n+          onChange={(e) => setRoom(e.target.value)}\r\n+        >\r\n+          <option value=\"\">Select a Room</option>\r\n+          {roomList.map((r) => (\r\n+            <option key={r} value={r}>\r\n+              {r}\r\n+            </option>\r\n+          ))}\r\n+        </select>\r\n+\r\n+        {/* New Room Creation */}\r\n+        <div className=\"flex gap-2 mb-4\">\r\n+          <input\r\n+            type=\"text\"\r\n+            placeholder=\"Create new room\"\r\n+            className=\"flex-1 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-green-400\"\r\n+            value={newRoom}\r\n+            onChange={(e) => setNewRoom(e.target.value)}\r\n+          />\r\n+          <button\r\n+            onClick={createRoom}\r\n+            className=\"bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600\"\r\n           >\r\n-            <option value=\"\">Select a Room</option>\r\n-            {roomList.map((r) => (\r\n-              <option key={r} value={r}>\r\n-                {r}\r\n-              </option>\r\n-            ))}\r\n-          </select>\r\n-\r\n-          <div className=\"flex gap-2\">\r\n-            <input\r\n-              type=\"text\"\r\n-              placeholder=\"Create new room\"\r\n-              className=\"w-full p-2 border rounded\"\r\n-              value={newRoom}\r\n-              onChange={(e) => setNewRoom(e.target.value)}\r\n-            />\r\n-            <button\r\n-              onClick={createRoom}\r\n-              className=\"bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600\"\r\n-            >\r\n-              +\r\n-            </button>\r\n-          </div>\r\n+            +\r\n+          </button>\r\n         </div>\r\n \r\n+        {/* Join Button */}\r\n         <button\r\n           className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n           onClick={handleJoin}\r\n         >\r\n"
                },
                {
                    "date": 1752161212839,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,94 +1,74 @@\n import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n import { io } from \"socket.io-client\";\r\n \r\n-// ⚠️ Singleton pattern for socket\r\n-const socket = io(\"http://localhost:3000\", { autoConnect: true });\r\n+const socket = io(\"http://localhost:3000\");\r\n \r\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n   const [roomList, setRoomList] = useState([]);\r\n   const [newRoom, setNewRoom] = useState(\"\");\r\n-\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n-    // 🧠 Listen for updated room list\r\n     socket.on(\"room-list\", (data) => {\r\n       setRoomList(data);\r\n     });\r\n \r\n-    // 🔌 Initial emit in case socket connected after page load\r\n-    socket.emit(\"get-rooms\");\r\n-\r\n-    // 🧹 Cleanup\r\n-    return () => {\r\n-      socket.off(\"room-list\");\r\n-    };\r\n+    return () => socket.disconnect();\r\n   }, []);\r\n \r\n-  // 🎯 Join selected room\r\n   const handleJoin = () => {\r\n-    if (!username.trim() || !room.trim()) {\r\n-      alert(\"Please enter username and select a room\");\r\n-      return;\r\n+    if (username.trim() && room) {\r\n+      navigate(`/chat?username=${username.trim()}&room=${room}`);\r\n     }\r\n-    navigate(`/chat?username=${username.trim()}&room=${room}`);\r\n   };\r\n \r\n-  // ➕ Create a new room\r\n   const createRoom = () => {\r\n-    const trimmedRoom = newRoom.trim();\r\n-    if (!trimmedRoom) return;\r\n-\r\n-    if (roomList.includes(trimmedRoom)) {\r\n-      alert(\"Room already exists!\");\r\n-      return;\r\n+    if (newRoom.trim() && !roomList.includes(newRoom)) {\r\n+      socket.emit(\"create-room\", newRoom.trim());\r\n+      setNewRoom(\"\");\r\n     }\r\n-\r\n-    socket.emit(\"create-room\", trimmedRoom);\r\n-    setNewRoom(\"\");\r\n-    setRoom(trimmedRoom);\r\n   };\r\n \r\n   return (\r\n     <div className=\"h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 to-blue-200\">\r\n-      <div className=\"bg-white p-6 rounded-lg shadow-lg w-full max-w-sm\">\r\n-        <h2 className=\"text-2xl font-bold mb-4 text-center text-blue-700\">\r\n+      <div className=\"bg-white p-8 rounded-2xl shadow-xl w-full max-w-md\">\r\n+        <h2 className=\"text-2xl font-bold mb-6 text-center text-blue-600\">\r\n           Join a Chat Room\r\n         </h2>\r\n \r\n-        {/* Username Input */}\r\n+        <label className=\"text-sm font-semibold text-gray-700\">Username</label>\r\n         <input\r\n           type=\"text\"\r\n           placeholder=\"Enter username\"\r\n-          className=\"w-full p-2 mb-4 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400\"\r\n+          className=\"w-full p-2 mt-1 mb-4 border rounded focus:outline-blue-400\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n \r\n-        {/* Room Selection */}\r\n+        <label className=\"text-sm font-semibold text-gray-700\">Select Room</label>\r\n         <select\r\n-          className=\"w-full p-2 mb-3 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400\"\r\n+          className=\"w-full p-2 mt-1 mb-4 border rounded bg-white focus:outline-blue-400\"\r\n           value={room}\r\n           onChange={(e) => setRoom(e.target.value)}\r\n         >\r\n-          <option value=\"\">Select a Room</option>\r\n+          <option value=\"\">-- Choose a room --</option>\r\n           {roomList.map((r) => (\r\n             <option key={r} value={r}>\r\n               {r}\r\n             </option>\r\n           ))}\r\n         </select>\r\n \r\n-        {/* New Room Creation */}\r\n-        <div className=\"flex gap-2 mb-4\">\r\n+        <label className=\"text-sm font-semibold text-gray-700\">Create New Room</label>\r\n+        <div className=\"flex mt-1 mb-4 gap-2\">\r\n           <input\r\n             type=\"text\"\r\n-            placeholder=\"Create new room\"\r\n-            className=\"flex-1 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-green-400\"\r\n+            placeholder=\"e.g. sports\"\r\n+            className=\"flex-1 p-2 border rounded focus:outline-blue-400\"\r\n             value={newRoom}\r\n             onChange={(e) => setNewRoom(e.target.value)}\r\n           />\r\n           <button\r\n@@ -98,14 +78,13 @@\n             +\r\n           </button>\r\n         </div>\r\n \r\n-        {/* Join Button */}\r\n         <button\r\n-          className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n           onClick={handleJoin}\r\n+          className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition\"\r\n         >\r\n-          Join\r\n+          Join Room\r\n         </button>\r\n       </div>\r\n     </div>\r\n   );\r\n"
                },
                {
                    "date": 1752161734103,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,8 +1,7 @@\n import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n import { io } from \"socket.io-client\";\r\n-\r\n const socket = io(\"http://localhost:3000\");\r\n \r\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n@@ -19,10 +18,10 @@\n     return () => socket.disconnect();\r\n   }, []);\r\n \r\n   const handleJoin = () => {\r\n-    if (username.trim() && room) {\r\n-      navigate(`/chat?username=${username.trim()}&room=${room}`);\r\n+    if (username && room) {\r\n+      navigate(`/chat?username=${username}&room=${room}`);\r\n     }\r\n   };\r\n \r\n   const createRoom = () => {\r\n@@ -32,59 +31,56 @@\n     }\r\n   };\r\n \r\n   return (\r\n-    <div className=\"h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 to-blue-200\">\r\n-      <div className=\"bg-white p-8 rounded-2xl shadow-xl w-full max-w-md\">\r\n-        <h2 className=\"text-2xl font-bold mb-6 text-center text-blue-600\">\r\n-          Join a Chat Room\r\n+    <div className=\"min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 to-blue-300\">\r\n+      <div className=\"bg-white w-full max-w-md p-8 rounded-2xl shadow-2xl\">\r\n+        <h2 className=\"text-2xl font-bold text-center text-blue-600 mb-6\">\r\n+          Welcome to QuickChat 💬\r\n         </h2>\r\n \r\n-        <label className=\"text-sm font-semibold text-gray-700\">Username</label>\r\n         <input\r\n           type=\"text\"\r\n-          placeholder=\"Enter username\"\r\n-          className=\"w-full p-2 mt-1 mb-4 border rounded focus:outline-blue-400\"\r\n+          placeholder=\"Enter your username\"\r\n+          className=\"w-full p-3 border border-gray-300 rounded-lg mb-4 focus:outline-none focus:ring-2 focus:ring-blue-500\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n \r\n-        <label className=\"text-sm font-semibold text-gray-700\">Select Room</label>\r\n         <select\r\n-          className=\"w-full p-2 mt-1 mb-4 border rounded bg-white focus:outline-blue-400\"\r\n+          className=\"w-full p-3 border border-gray-300 rounded-lg mb-4 bg-white focus:outline-none focus:ring-2 focus:ring-blue-500\"\r\n           value={room}\r\n           onChange={(e) => setRoom(e.target.value)}\r\n         >\r\n-          <option value=\"\">-- Choose a room --</option>\r\n+          <option value=\"\">Select a Room</option>\r\n           {roomList.map((r) => (\r\n             <option key={r} value={r}>\r\n               {r}\r\n             </option>\r\n           ))}\r\n         </select>\r\n \r\n-        <label className=\"text-sm font-semibold text-gray-700\">Create New Room</label>\r\n-        <div className=\"flex mt-1 mb-4 gap-2\">\r\n+        <div className=\"flex mb-4 gap-2\">\r\n           <input\r\n             type=\"text\"\r\n-            placeholder=\"e.g. sports\"\r\n-            className=\"flex-1 p-2 border rounded focus:outline-blue-400\"\r\n+            placeholder=\"Create new room\"\r\n+            className=\"flex-1 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500\"\r\n             value={newRoom}\r\n             onChange={(e) => setNewRoom(e.target.value)}\r\n           />\r\n           <button\r\n             onClick={createRoom}\r\n-            className=\"bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600\"\r\n+            className=\"bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600 transition\"\r\n           >\r\n             +\r\n           </button>\r\n         </div>\r\n \r\n         <button\r\n           onClick={handleJoin}\r\n-          className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition\"\r\n+          className=\"w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition\"\r\n         >\r\n-          Join Room\r\n+          Join Chat\r\n         </button>\r\n       </div>\r\n     </div>\r\n   );\r\n"
                },
                {
                    "date": 1752162531685,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,8 @@\n import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n import { io } from \"socket.io-client\";\r\n+\r\n const socket = io(\"http://localhost:3000\");\r\n \r\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n@@ -10,8 +11,10 @@\n   const [newRoom, setNewRoom] = useState(\"\");\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n+    socket.emit(\"get-room-list\");\r\n+\r\n     socket.on(\"room-list\", (data) => {\r\n       setRoomList(data);\r\n     });\r\n \r\n@@ -24,63 +27,50 @@\n     }\r\n   };\r\n \r\n   const createRoom = () => {\r\n-    if (newRoom.trim() && !roomList.includes(newRoom)) {\r\n+    if (newRoom && !roomList.includes(newRoom)) {\r\n       socket.emit(\"create-room\", newRoom.trim());\r\n       setNewRoom(\"\");\r\n     }\r\n   };\r\n \r\n   return (\r\n-    <div className=\"min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 to-blue-300\">\r\n-      <div className=\"bg-white w-full max-w-md p-8 rounded-2xl shadow-2xl\">\r\n-        <h2 className=\"text-2xl font-bold text-center text-blue-600 mb-6\">\r\n-          Welcome to QuickChat 💬\r\n-        </h2>\r\n-\r\n+    <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n+      <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n+        <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n         <input\r\n           type=\"text\"\r\n-          placeholder=\"Enter your username\"\r\n-          className=\"w-full p-3 border border-gray-300 rounded-lg mb-4 focus:outline-none focus:ring-2 focus:ring-blue-500\"\r\n+          placeholder=\"Enter username\"\r\n+          className=\"w-full p-2 mb-3 border rounded\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n-\r\n         <select\r\n-          className=\"w-full p-3 border border-gray-300 rounded-lg mb-4 bg-white focus:outline-none focus:ring-2 focus:ring-blue-500\"\r\n+          className=\"w-full p-2 mb-2 border rounded\"\r\n           value={room}\r\n           onChange={(e) => setRoom(e.target.value)}\r\n         >\r\n           <option value=\"\">Select a Room</option>\r\n           {roomList.map((r) => (\r\n-            <option key={r} value={r}>\r\n-              {r}\r\n-            </option>\r\n+            <option key={r} value={r}>{r}</option>\r\n           ))}\r\n         </select>\r\n-\r\n-        <div className=\"flex mb-4 gap-2\">\r\n+        <div className=\"flex gap-2\">\r\n           <input\r\n             type=\"text\"\r\n-            placeholder=\"Create new room\"\r\n-            className=\"flex-1 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500\"\r\n+            placeholder=\"New room\"\r\n+            className=\"w-full p-2 border rounded\"\r\n             value={newRoom}\r\n             onChange={(e) => setNewRoom(e.target.value)}\r\n           />\r\n-          <button\r\n-            onClick={createRoom}\r\n-            className=\"bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600 transition\"\r\n-          >\r\n-            +\r\n-          </button>\r\n+          <button onClick={createRoom} className=\"bg-green-500 text-white px-4 py-2 rounded\">+</button>\r\n         </div>\r\n-\r\n         <button\r\n+          className=\"w-full bg-blue-500 text-white mt-4 py-2 rounded\"\r\n           onClick={handleJoin}\r\n-          className=\"w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition\"\r\n         >\r\n-          Join Chat\r\n+          Join\r\n         </button>\r\n       </div>\r\n     </div>\r\n   );\r\n"
                },
                {
                    "date": 1752163594307,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,25 +1,24 @@\n+// Login.jsx\r\n import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n-import { io } from \"socket.io-client\";\r\n+import socket from \"../socket\";\r\n \r\n-const socket = io(\"http://localhost:3000\");\r\n-\r\n export default function Login() {\r\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n   const [roomList, setRoomList] = useState([]);\r\n   const [newRoom, setNewRoom] = useState(\"\");\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n-    socket.emit(\"get-room-list\");\r\n+    if (!socket.connected) socket.connect();\r\n \r\n-    socket.on(\"room-list\", (data) => {\r\n-      setRoomList(data);\r\n-    });\r\n+    socket.on(\"room-list\", (data) => setRoomList(data));\r\n \r\n-    return () => socket.disconnect();\r\n+    return () => {\r\n+      socket.off(\"room-list\");\r\n+    };\r\n   }, []);\r\n \r\n   const handleJoin = () => {\r\n     if (username && room) {\r\n@@ -27,9 +26,9 @@\n     }\r\n   };\r\n \r\n   const createRoom = () => {\r\n-    if (newRoom && !roomList.includes(newRoom)) {\r\n+    if (newRoom.trim()) {\r\n       socket.emit(\"create-room\", newRoom.trim());\r\n       setNewRoom(\"\");\r\n     }\r\n   };\r\n@@ -44,30 +43,40 @@\n           className=\"w-full p-2 mb-3 border rounded\"\r\n           value={username}\r\n           onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n-        <select\r\n-          className=\"w-full p-2 mb-2 border rounded\"\r\n-          value={room}\r\n-          onChange={(e) => setRoom(e.target.value)}\r\n-        >\r\n-          <option value=\"\">Select a Room</option>\r\n-          {roomList.map((r) => (\r\n-            <option key={r} value={r}>{r}</option>\r\n-          ))}\r\n-        </select>\r\n-        <div className=\"flex gap-2\">\r\n-          <input\r\n-            type=\"text\"\r\n-            placeholder=\"New room\"\r\n-            className=\"w-full p-2 border rounded\"\r\n-            value={newRoom}\r\n-            onChange={(e) => setNewRoom(e.target.value)}\r\n-          />\r\n-          <button onClick={createRoom} className=\"bg-green-500 text-white px-4 py-2 rounded\">+</button>\r\n+\r\n+        <div className=\"mb-4\">\r\n+          <select\r\n+            className=\"w-full p-2 mb-2 border rounded\"\r\n+            value={room}\r\n+            onChange={(e) => setRoom(e.target.value)}\r\n+          >\r\n+            <option value=\"\">Select a Room</option>\r\n+            {roomList.map((r) => (\r\n+              <option key={r} value={r}>{r}</option>\r\n+            ))}\r\n+          </select>\r\n+\r\n+          <div className=\"flex gap-2\">\r\n+            <input\r\n+              type=\"text\"\r\n+              placeholder=\"Create new room\"\r\n+              className=\"w-full p-2 border rounded\"\r\n+              value={newRoom}\r\n+              onChange={(e) => setNewRoom(e.target.value)}\r\n+            />\r\n+            <button\r\n+              onClick={createRoom}\r\n+              className=\"bg-green-500 text-white px-4 py-2 rounded\"\r\n+            >\r\n+              +\r\n+            </button>\r\n+          </div>\r\n         </div>\r\n+\r\n         <button\r\n-          className=\"w-full bg-blue-500 text-white mt-4 py-2 rounded\"\r\n+          className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n           onClick={handleJoin}\r\n         >\r\n           Join\r\n         </button>\r\n"
                },
                {
                    "date": 1752326208917,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,4 @@\n-// Login.jsx\r\n import React, { useEffect, useState } from \"react\";\r\n import { useNavigate } from \"react-router-dom\";\r\n import socket from \"../socket\";\r\n \r\n@@ -36,8 +35,9 @@\n   return (\r\n     <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n       <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n         <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n+        \r\n         <input\r\n           type=\"text\"\r\n           placeholder=\"Enter username\"\r\n           className=\"w-full p-2 mb-3 border rounded\"\r\n@@ -52,9 +52,11 @@\n             onChange={(e) => setRoom(e.target.value)}\r\n           >\r\n             <option value=\"\">Select a Room</option>\r\n             {roomList.map((r) => (\r\n-              <option key={r} value={r}>{r}</option>\r\n+              <option key={r.name} value={r.name}>\r\n+                {r.name} — {r.description}\r\n+              </option>\r\n             ))}\r\n           </select>\r\n \r\n           <div className=\"flex gap-2\">\r\n"
                },
                {
                    "date": 1753980963158,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,8 +6,9 @@\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n   const [roomList, setRoomList] = useState([]);\r\n   const [newRoom, setNewRoom] = useState(\"\");\r\n+  const [error, setError] = useState(\"\");\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n     if (!socket.connected) socket.connect();\r\n@@ -19,11 +20,23 @@\n     };\r\n   }, []);\r\n \r\n   const handleJoin = () => {\r\n-    if (username && room) {\r\n-      navigate(`/chat?username=${username}&room=${room}`);\r\n+    if (!username || !room) {\r\n+      setError(\"Username and Room are required\");\r\n+      return;\r\n     }\r\n+\r\n+    // ✅ Check with backend if username is already taken\r\n+    socket.emit(\"checkUsername\", username, (isAvailable) => {\r\n+      if (isAvailable) {\r\n+        socket.emit(\"setUsername\", username);\r\n+        localStorage.setItem(\"username\", username);\r\n+        navigate(`/chat?username=${username}&room=${room}`);\r\n+      } else {\r\n+        setError(\"❌ Username already taken. Please choose another.\");\r\n+      }\r\n+    });\r\n   };\r\n \r\n   const createRoom = () => {\r\n     if (newRoom.trim()) {\r\n@@ -35,16 +48,20 @@\n   return (\r\n     <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n       <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n         <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n-        \r\n+\r\n         <input\r\n           type=\"text\"\r\n           placeholder=\"Enter username\"\r\n-          className=\"w-full p-2 mb-3 border rounded\"\r\n+          className=\"w-full p-2 mb-2 border rounded\"\r\n           value={username}\r\n-          onChange={(e) => setUsername(e.target.value)}\r\n+          onChange={(e) => {\r\n+            setUsername(e.target.value);\r\n+            setError(\"\");\r\n+          }}\r\n         />\r\n+        {error && <p className=\"text-red-500 text-sm mb-2\">{error}</p>}\r\n \r\n         <div className=\"mb-4\">\r\n           <select\r\n             className=\"w-full p-2 mb-2 border rounded\"\r\n"
                },
                {
                    "date": 1753981499665,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,8 @@\n   const [username, setUsername] = useState(\"\");\r\n   const [room, setRoom] = useState(\"\");\r\n   const [roomList, setRoomList] = useState([]);\r\n   const [newRoom, setNewRoom] = useState(\"\");\r\n-  const [error, setError] = useState(\"\");\r\n   const navigate = useNavigate();\r\n \r\n   useEffect(() => {\r\n     if (!socket.connected) socket.connect();\r\n@@ -20,23 +19,11 @@\n     };\r\n   }, []);\r\n \r\n   const handleJoin = () => {\r\n-    if (!username || !room) {\r\n-      setError(\"Username and Room are required\");\r\n-      return;\r\n+    if (username && room) {\r\n+      navigate(`/chat?username=${username}&room=${room}`);\r\n     }\r\n-\r\n-    // ✅ Check with backend if username is already taken\r\n-    socket.emit(\"checkUsername\", username, (isAvailable) => {\r\n-      if (isAvailable) {\r\n-        socket.emit(\"setUsername\", username);\r\n-        localStorage.setItem(\"username\", username);\r\n-        navigate(`/chat?username=${username}&room=${room}`);\r\n-      } else {\r\n-        setError(\"❌ Username already taken. Please choose another.\");\r\n-      }\r\n-    });\r\n   };\r\n \r\n   const createRoom = () => {\r\n     if (newRoom.trim()) {\r\n@@ -48,20 +35,16 @@\n   return (\r\n     <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n       <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n         <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n-\r\n+        \r\n         <input\r\n           type=\"text\"\r\n           placeholder=\"Enter username\"\r\n-          className=\"w-full p-2 mb-2 border rounded\"\r\n+          className=\"w-full p-2 mb-3 border rounded\"\r\n           value={username}\r\n-          onChange={(e) => {\r\n-            setUsername(e.target.value);\r\n-            setError(\"\");\r\n-          }}\r\n+          onChange={(e) => setUsername(e.target.value)}\r\n         />\r\n-        {error && <p className=\"text-red-500 text-sm mb-2\">{error}</p>}\r\n \r\n         <div className=\"mb-4\">\r\n           <select\r\n             className=\"w-full p-2 mb-2 border rounded\"\r\n"
                },
                {
                    "date": 1753982297861,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -18,14 +18,24 @@\n       socket.off(\"room-list\");\r\n     };\r\n   }, []);\r\n \r\n-  const handleJoin = () => {\r\n-    if (username && room) {\r\n+const handleJoin = () => {\r\n+  if (username && room) {\r\n+    socket.emit(\"join-room\", { username, room });\r\n+\r\n+    // Wait for confirmation or error\r\n+    socket.once(\"join-success\", () => {\r\n       navigate(`/chat?username=${username}&room=${room}`);\r\n-    }\r\n-  };\r\n+    });\r\n \r\n+    socket.once(\"error-message\", (msg) => {\r\n+      alert(msg); // or set an error state to display in UI\r\n+    });\r\n+  }\r\n+};\r\n+\r\n+\r\n   const createRoom = () => {\r\n     if (newRoom.trim()) {\r\n       socket.emit(\"create-room\", newRoom.trim());\r\n       setNewRoom(\"\");\r\n"
                },
                {
                    "date": 1753983594427,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -19,23 +19,30 @@\n     };\r\n   }, []);\r\n \r\n const handleJoin = () => {\r\n-  if (username && room) {\r\n-    socket.emit(\"join-room\", { username, room });\r\n+  if (!username || !room) return;\r\n \r\n-    // Wait for confirmation or error\r\n-    socket.once(\"join-success\", () => {\r\n-      navigate(`/chat?username=${username}&room=${room}`);\r\n-    });\r\n+  // Emit the join request\r\n+  socket.emit(\"join-room\", { username, room });\r\n \r\n-    socket.once(\"error-message\", (msg) => {\r\n-      alert(msg); // or set an error state to display in UI\r\n-    });\r\n-  }\r\n+  // Remove old listeners to prevent duplicate behavior\r\n+  socket.off(\"user-list\");\r\n+  socket.off(\"error-message\");\r\n+\r\n+  // If error like duplicate username, show alert\r\n+  socket.once(\"error-message\", (msg) => {\r\n+    alert(msg); // e.g. \"Username already taken\"\r\n+  });\r\n+\r\n+  // Only navigate if joining succeeded\r\n+  socket.once(\"user-list\", () => {\r\n+    navigate(`/chat?username=${username}&room=${room}`);\r\n+  });\r\n };\r\n \r\n \r\n+\r\n   const createRoom = () => {\r\n     if (newRoom.trim()) {\r\n       socket.emit(\"create-room\", newRoom.trim());\r\n       setNewRoom(\"\");\r\n"
                },
                {
                    "date": 1753983832270,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -21,28 +21,38 @@\n \r\n const handleJoin = () => {\r\n   if (!username || !room) return;\r\n \r\n-  // Emit the join request\r\n-  socket.emit(\"join-room\", { username, room });\r\n-\r\n-  // Remove old listeners to prevent duplicate behavior\r\n+  // Remove any previous listeners to avoid multiple triggers\r\n   socket.off(\"user-list\");\r\n   socket.off(\"error-message\");\r\n \r\n-  // If error like duplicate username, show alert\r\n-  socket.once(\"error-message\", (msg) => {\r\n-    alert(msg); // e.g. \"Username already taken\"\r\n-  });\r\n+  // Listen for response\r\n+  const onError = (msg) => {\r\n+    alert(msg);\r\n+    cleanup(); // cleanup after alert\r\n+  };\r\n \r\n-  // Only navigate if joining succeeded\r\n-  socket.once(\"user-list\", () => {\r\n+  const onSuccess = () => {\r\n+    cleanup(); // cleanup before navigating\r\n     navigate(`/chat?username=${username}&room=${room}`);\r\n-  });\r\n+  };\r\n+\r\n+  const cleanup = () => {\r\n+    socket.off(\"error-message\", onError);\r\n+    socket.off(\"user-list\", onSuccess);\r\n+  };\r\n+\r\n+  socket.once(\"error-message\", onError);\r\n+  socket.once(\"user-list\", onSuccess);\r\n+\r\n+  // Emit the join request\r\n+  socket.emit(\"join-room\", { username, room });\r\n };\r\n \r\n \r\n \r\n+\r\n   const createRoom = () => {\r\n     if (newRoom.trim()) {\r\n       socket.emit(\"create-room\", newRoom.trim());\r\n       setNewRoom(\"\");\r\n"
                },
                {
                    "date": 1753984279521,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -17,37 +17,49 @@\n     return () => {\r\n       socket.off(\"room-list\");\r\n     };\r\n   }, []);\r\n-\r\n const handleJoin = () => {\r\n-  if (!username || !room) return;\r\n+  // Validation for empty username\r\n+  if (!username.trim()) {\r\n+    alert(\"Please enter a username.\");\r\n+    return;\r\n+  }\r\n \r\n-  // Remove any previous listeners to avoid multiple triggers\r\n+  // Validation for empty room\r\n+  if (!room.trim()) {\r\n+    alert(\"Please select a room.\");\r\n+    return;\r\n+  }\r\n+\r\n+  // Clear previous listeners to avoid duplication\r\n   socket.off(\"user-list\");\r\n   socket.off(\"error-message\");\r\n \r\n-  // Listen for response\r\n+  // Handle error from server (e.g., duplicate username)\r\n   const onError = (msg) => {\r\n     alert(msg);\r\n-    cleanup(); // cleanup after alert\r\n+    cleanup();\r\n   };\r\n \r\n+  // Handle success (navigate to chat)\r\n   const onSuccess = () => {\r\n-    cleanup(); // cleanup before navigating\r\n+    cleanup();\r\n     navigate(`/chat?username=${username}&room=${room}`);\r\n   };\r\n \r\n+  // Cleanup listeners\r\n   const cleanup = () => {\r\n     socket.off(\"error-message\", onError);\r\n     socket.off(\"user-list\", onSuccess);\r\n   };\r\n \r\n+  // Set up listeners\r\n   socket.once(\"error-message\", onError);\r\n   socket.once(\"user-list\", onSuccess);\r\n \r\n-  // Emit the join request\r\n-  socket.emit(\"join-room\", { username, room });\r\n+  // Emit join request\r\n+  socket.emit(\"join-room\", { username: username.trim(), room: room.trim() });\r\n };\r\n \r\n \r\n \r\n"
                },
                {
                    "date": 1754056064417,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,132 @@\n+import React, { useEffect, useState } from \"react\";\r\n+import { useNavigate } from \"react-router-dom\";\r\n+import socket from \"../socket\";\r\n+\r\n+export default function Login() {\r\n+  const [username, setUsername] = useState(\"\");\r\n+  const [room, setRoom] = useState(\"\");\r\n+  const [roomList, setRoomList] = useState([]);\r\n+  const [newRoom, setNewRoom] = useState(\"\");\r\n+  const navigate = useNavigate();\r\n+\r\n+ useEffect(() => {\r\n+  if (!socket.connected) socket.connect();\r\n+\r\n+  // Request list from server\r\n+  socket.emit(\"get-room-list\");\r\n+\r\n+  // Listen for the list\r\n+  socket.on(\"room-list\", (data) => setRoomList(data));\r\n+\r\n+  return () => {\r\n+    socket.off(\"room-list\");\r\n+  };\r\n+}, []);\r\n+\r\n+const handleJoin = () => {\r\n+  // Validation for empty username\r\n+  if (!username.trim()) {\r\n+    alert(\"Please enter a username.\");\r\n+    return;\r\n+  }\r\n+\r\n+  // Validation for empty room\r\n+  if (!room.trim()) {\r\n+    alert(\"Please select a room.\");\r\n+    return;\r\n+  }\r\n+\r\n+  // Clear previous listeners to avoid duplication\r\n+  socket.off(\"user-list\");\r\n+  socket.off(\"error-message\");\r\n+\r\n+  // Handle error from server (e.g., duplicate username)\r\n+  const onError = (msg) => {\r\n+    alert(msg);\r\n+    cleanup();\r\n+  };\r\n+\r\n+  // Handle success (navigate to chat)\r\n+  const onSuccess = () => {\r\n+    cleanup();\r\n+    navigate(`/chat?username=${username}&room=${room}`);\r\n+  };\r\n+\r\n+  // Cleanup listeners\r\n+  const cleanup = () => {\r\n+    socket.off(\"error-message\", onError);\r\n+    socket.off(\"user-list\", onSuccess);\r\n+  };\r\n+\r\n+  // Set up listeners\r\n+  socket.once(\"error-message\", onError);\r\n+  socket.once(\"user-list\", onSuccess);\r\n+\r\n+  // Emit join request\r\n+  socket.emit(\"join-room\", { username: username.trim(), room: room.trim() });\r\n+};\r\n+\r\n+\r\n+\r\n+\r\n+  const createRoom = () => {\r\n+    if (newRoom.trim()) {\r\n+      socket.emit(\"create-room\", newRoom.trim());\r\n+      setNewRoom(\"\");\r\n+    }\r\n+  };\r\n+\r\n+  return (\r\n+    <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n+      <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n+        <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n+        \r\n+        <input\r\n+          type=\"text\"\r\n+          placeholder=\"Enter username\"\r\n+          className=\"w-full p-2 mb-3 border rounded\"\r\n+          value={username}\r\n+          onChange={(e) => setUsername(e.target.value)}\r\n+        />\r\n+\r\n+        <div className=\"mb-4\">\r\n+          <select\r\n+            className=\"w-full p-2 mb-2 border rounded\"\r\n+            value={room}\r\n+            onChange={(e) => setRoom(e.target.value)}\r\n+          >\r\n+            <option value=\"\">Select a Room</option>\r\n+            {roomList.map((r) => (\r\n+              <option key={r.name} value={r.name}>\r\n+                {r.name} — {r.description}\r\n+              </option>\r\n+            ))}\r\n+          </select>\r\n+\r\n+          <div className=\"flex gap-2\">\r\n+            <input\r\n+              type=\"text\"\r\n+              placeholder=\"Create new room\"\r\n+              className=\"w-full p-2 border rounded\"\r\n+              value={newRoom}\r\n+              onChange={(e) => setNewRoom(e.target.value)}\r\n+            />\r\n+            <button\r\n+              onClick={createRoom}\r\n+              className=\"bg-green-500 text-white px-4 py-2 rounded\"\r\n+            >\r\n+              +\r\n+            </button>\r\n+          </div>\r\n+        </div>\r\n+\r\n+        <button\r\n+          className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n+          onClick={handleJoin}\r\n+        >\r\n+          Join\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  );\r\n+}\r\n"
                }
            ],
            "date": 1752155001359,
            "name": "Commit-0",
            "content": "import React, { useState } from \"react\";\r\nimport { useNavigate } from \"react-router-dom\";\r\n\r\nexport default function Login() {\r\n  const [username, setUsername] = useState(\"\");\r\n  const [room, setRoom] = useState(\"\");\r\n  const navigate = useNavigate();\r\n\r\n  const handleJoin = () => {\r\n    if (username && room) {\r\n      navigate(`/chat?username=${username}&room=${room}`);\r\n    }\r\n  };\r\n\r\n  return (\r\n    <div className=\"h-screen flex items-center justify-center bg-blue-100\">\r\n      <div className=\"bg-white p-6 rounded-lg shadow-lg w-96\">\r\n        <h2 className=\"text-xl font-bold mb-4 text-center\">Join a Chat Room</h2>\r\n        <input\r\n          type=\"text\"\r\n          placeholder=\"Enter username\"\r\n          className=\"w-full p-2 mb-3 border rounded\"\r\n          value={username}\r\n          onChange={(e) => setUsername(e.target.value)}\r\n        />\r\n        <input\r\n          type=\"text\"\r\n          placeholder=\"Enter room\"\r\n          className=\"w-full p-2 mb-4 border rounded\"\r\n          value={room}\r\n          onChange={(e) => setRoom(e.target.value)}\r\n        />\r\n        <button\r\n          className=\"w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600\"\r\n          onClick={handleJoin}\r\n        >\r\n          Join\r\n        </button>\r\n      </div>\r\n    </div>\r\n  );\r\n}\r\n"
        }
    ]
}